
def count_ways_to_summit(n):

    prev = 0
    current = 0

    for i in range(1, n+ 1):

        temp = prev
        prev = current

        if prev == 0:
            current += 1
            prev = 1
        else:
            current += temp

    return current


print(count_ways_to_summit(6))
